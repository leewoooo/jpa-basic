spring:
  datasource:
    driver-class-name: org.h2.Driver
    username: sa
    password:
    url: jdbc:h2:tcp://localhost/~/dev/program/h2/basic

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true